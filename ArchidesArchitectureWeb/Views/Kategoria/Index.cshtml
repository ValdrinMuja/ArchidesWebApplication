@model IEnumerable<ArchidesArchitectureWeb.Kategoria>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Kategoria1)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Activ)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Kategoria1)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Activ)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.KategoriaID }) |
            @Html.ActionLink("Details", "Details", new { id=item.KategoriaID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.KategoriaID })
        </td>
    </tr>
}

</table>
