@model IEnumerable<ArchidesArchitectureWeb.Roli>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Roli1)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Activ)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Roli1)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Activ)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.RoliID }) |
            @Html.ActionLink("Details", "Details", new { id=item.RoliID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.RoliID })
        </td>
    </tr>
}

</table>
